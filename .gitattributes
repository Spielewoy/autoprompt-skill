# Preserve POSIX script line endings for Windows contributors and any
# cross-platform checkout, so `#!/usr/bin/env node` cannot become `node\r`.
*.cjs text eol=lf
*.sh text eol=lf
bin/** text eol=lf
